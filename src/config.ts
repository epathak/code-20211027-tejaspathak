/** A single file enlisting all the configuration used within the application. */

/** The variable for configuring the path of environment file to be loaded for local development. */
export const ENV_FILE_PATH = 'env_sample.json';
/** The variable for configuring the path of folder containing the static files for frontend. */
export const STATIC_FILES_PATH = 'documentation';
